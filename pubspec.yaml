name: iv_project_core
description: "IV Project Core"
version: 0.0.4
homepage:
publish_to: 'none'

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # iv_project_api_core:
  #   path: ../iv_project_api_core
  
  iv_project_api_core:
    git:
      url: https://github.com/faequl96/iv_project_api_core.git
      ref: 0.0.2

  # quick_dev_sdk:
  #   path: ../quick_dev_sdk
  
  quick_dev_sdk:
    git:
      url: https://github.com/faequl96/quick_dev_sdk.git
      ref: 0.0.5
  
  go_router: ^16.0.0

  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
